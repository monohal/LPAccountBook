<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hal.lpaccountbook" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon" >

        <activity
            android:name=".LPAccountBookActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ListViewActivity">
        </activity>

        <activity
            android:name=".setDataActivity">
        </activity>

        <activity
            android:name=".StackedBarViewActivity">
        </activity>

        <provider
            android:authorities="com.example.hal.lpaccountbook"
            android:name="com.example.hal.lpaccountbook.DataProvider"
            android:exported="false">
        </provider>
    </application>

</manifest>
